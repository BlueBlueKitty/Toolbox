# 像素时序查看器功能说明

## 功能概述

像素时序查看器是一个用于查看和分析时序图像数据中像素值变化的工具。

## 功能特点

### 1. 图像加载与管理

- 支持从文件夹批量加载时序图像
- 支持的格式：PNG、JPG、JPEG、BMP、TIF、TIFF
- 对于TIFF格式，支持读取地理信息和多波段数据
- 自动检查图像尺寸和波段数一致性

### 2. 图像排序

- 按文件名排序
- 支持正序和倒序两种排序方式
- 动态切换排序方式

### 3. 图像查看功能

- **鼠标滚轮缩放**：滚轮向上放大，向下缩小
- **鼠标拖动**：按住鼠标左键拖动图像
- **双击重置**：双击图像恢复到适应窗口大小
- **右键菜单**：
  - 选择颜色映射（colormap）：gray、viridis、plasma、jet等20种配色方案
  - 适应窗口、放大、缩小等快捷操作
- **图像切换**：
  - 使用"上一张"/"下一张"按钮
  - 拖动滑块快速跳转
  - 实时显示当前图像索引

### 4. 像素时序分析

- **点击选择像素**：在图像上点击任意位置选择像素
- **时序曲线绘制**：
  - 自动提取所有时序图像中该像素的值
  - 绘制像素值随时间（图像索引）的变化曲线
  - 当前显示图像的像素值用红色标记突出显示

### 5. 多波段支持

- **单波段图像**：绘制单条像素值曲线
- **多波段图像**：为每个波段分别绘制曲线
- **RGB图像（3波段）**：
  - 分别绘制R、G、B三个波段的曲线
  - 额外计算并绘制灰度值曲线（灰度 = 0.299×R + 0.587×G + 0.114×B）

## 使用步骤

1. **打开工具**
   - 启动主程序后，点击"像素时序查看器"按钮

2. **加载图像**
   - 点击"打开图像文件夹"按钮
   - 选择包含时序图像的文件夹
   - 系统自动加载并检查图像一致性

3. **查看图像**
   - 使用滑块或按钮切换不同时刻的图像
   - 使用鼠标滚轮缩放、拖动查看细节
   - 右键选择合适的colormap以更好地显示数据

4. **分析像素时序**
   - 在图像上点击感兴趣的像素位置
   - 右侧自动显示该像素的时序曲线
   - 红色标记显示当前图像的像素值
   - 切换图像时，红色标记会随之更新

## 注意事项

1. **图像一致性**：
   - 所有时序图像必须具有相同的尺寸（宽度×高度）
   - 所有时序图像必须具有相同的波段数
   - 不符合要求的图像会被跳过并提示

2. **性能优化**：
   - 建议单次加载的图像数量不超过1000张
   - 大尺寸图像可能需要较长加载时间

3. **数据类型**：
   - 普通图像（PNG/JPG等）：8位整数
   - TIFF图像：支持多种数据类型，自动归一化显示

## 技术实现

### 核心组件

- **ImageViewer**：自定义图像查看器组件，封装了缩放、拖动、颜色映射等功能
- **PixelTimeSeriesViewerDialog**：主对话框，管理图像加载和时序分析
- **Matplotlib**：用于绘制时序曲线
- **GDAL**：用于读取地理TIFF数据
- **PIL**：用于读取普通图像格式

### 可复用性

ImageViewer组件已封装为独立模块，可在其他功能中复用：

```python
from src.widgets import ImageViewer

viewer = ImageViewer()
viewer.set_image_from_array(image_array)
viewer.pixel_clicked.connect(callback_function)
```

## 依赖包

确保安装以下Python包：

- pyside6
- matplotlib
- pillow
- numpy
- gdal
